#!/bin/bash

source ~/.bashrc

name="."

if (( $# >= 1 )); then
    name=$1
fi

vim "+ cd $name" \
  "+ set tags+=tags;,./tags" \
  "+ set path+=$name/**" \
  $name/.label_0_0 $name/.project_0_0 $name/.tmp_0_0
